<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/taobao_icon"
        android:label="我的淘宝"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar"
        tools:targetApi="31">
        <activity
            android:name=".Activitise.CoverActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Fragments.ProductDetailsActivity" />
        <activity android:name=".Activitise.RegisterActivity" />
        <activity android:name=".MainActivity"/>
        <activity android:name=".Activitise.LoginActivity"/>
        <receiver
            android:name=".Shop.ShopFragment$CartUpdateReceiver"
            android:exported="false" />

    </application>

</manifest>